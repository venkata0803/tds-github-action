name: Cache Test

on:
  push:
    branches:
      - main

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.cache
          key: cache-8fd94bb

      - name: prime-cache-8fd94bb
        run: echo "Cache hit? ${{ steps.cache.outputs.cache-hit }}"
